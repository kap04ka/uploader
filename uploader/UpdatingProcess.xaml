<Window x:Class="uploader.UpdatingProcess"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:uploader"
        mc:Ignorable="d"
        Title="UpdatingProcess" Height="200" Width="500">
    <Grid>
        <TextBlock Text="Обновление программы в процессе" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="10, 30, 10, 10"/>
        <ProgressBar Height="30" Value="{Binding Path=progress}" Minimum="0" Maximum="100" VerticalAlignment="Center" Margin="20"/>
        <TextBlock Text="{Binding Path=speed}" VerticalAlignment="Center" Margin="20, 70, 10, 10"/>
    </Grid>
</Window>
